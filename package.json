{
  "name": "lumo-framework",
  "version": "0.1.0-alpha.4",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@eslint/js": "^9.27.0",
    "@typescript-eslint/eslint-plugin": "^8.32.1",
    "@typescript-eslint/parser": "^8.32.1",
    "eslint": "^9.27.0",
    "eslint-config-prettier": "^10.1.5",
    "prettier": "^3.5.3",
    "typescript": "^5.0.0"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/lumo-framework/monorepo.git"
  },
  "homepage": "https://lumo-framework.dev",
  "description": "Lumo Framework auto-discovers routes and subscribers, compiles them to cloud-native functions, and deploys to AWS or Cloudflare.",
  "license": "MIT",
  "publishConfig": {
    "tag": "next"
  }
}
